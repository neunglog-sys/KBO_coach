# MongoDB 접속 정보 — 실제 값은 .env 에 넣으세요 (.env 는 git에 안 올라감)
# Atlas 연결 문자열: Atlas → Cluster Connect → Drivers → Python 에서 복사,
# <password> 자리를 본인 DB 유저 비밀번호로 교체.
MONGO_URI=mongodb+srv://<user>:<password>@cluster0.xxxxx.mongodb.net/?appName=Cluster0
MONGO_DB=kbo
